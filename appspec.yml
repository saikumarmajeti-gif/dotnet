version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/dotnetapp
    overwrite: true

hooks:
  BeforeInstall:
    - location: scripts/stop.sh
      timeout: 60
      runas: root

  AfterInstall:
    - location: scripts/start.sh
      timeout: 60
      runas: root

